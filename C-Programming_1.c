//C Programming Begins

/* #include içe aktarır => <kütüphane.adi>
   #define variable yerine en üst kısımda kullanılabilir.
   
   ';' bu dilde vazgeçilmez.
   
   \n : bir satır alta geçer.
   \t : bir tab boşluk atar.

   int : tam sayı  '%d' 
   float : ondalıklı sayı '%f'
   double : daha derin kesirli sayılar '%1f'
   char : harfler (tırnak içi yazılır) '%s'

   printf : Ekrana yazdırır.
   = printf('Enter a number');
   scanf : Kullanıcıdan giriş bekler. Girilen değeri almak için değişken adı başına & eklenir.
   = scanf('%d',&number);

   printf(sizeof(degiskenadi)) : değişkenin kapladığı alanı verir.

    */